        <nav class="navbar navbar-expand-lg" id="nav1">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_home') }}">
                    <img id="logoNav" src="{{ asset('img/Logo.png') }}" alt="logo Luttes Enchantées!">
                </a>
                <button class="navbar-toggler navbarLink" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon" id="navbar-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav me-auto">
                        {% if app.user %}
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_members_home') }}">Accueil</a>
                            </li>
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_event') }}">Agenda</a>
                            </li>
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_song') }}">Chants</a>
                            </li>
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_user_list') }}">Liste</a>
                            </li>
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_chatItems') }}">Messages</a>
                            </li>
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_sharing_item') }}">Echanges</a>
                            </li>                            
                            {% if app.user.roles[0] == "ROLE_DATES" or app.user.roles[0] == "ROLE_ADMIN" %}
                            <li class="nav-item m-auto">
                                    <a class="nav-link navbarLink" href="{{ path('app_manage_event') }}">Gérer les dates</a>
                                </li>
                            {% endif %}
                            {% if app.user.roles[0] == "ROLE_CHANTS" or app.user.roles[0] == "ROLE_ADMIN" %}
                            <li class="nav-item m-auto">
                                    <a class="nav-link navbarLink" href="{{ path('app_manage_songs') }}">Gérer les chants</a>
                                </li>
                            {% endif %}
                            {% if app.user.roles[0] == "ROLE_ADMIN" %}
                            <li class="nav-item m-auto">
                                    <a class="nav-link navbarLink" href="{{ path('app_new_user') }}">Gérer les membres</a>
                                </li>
                            {% endif %}
                        {% else %}
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_contact') }}">Contact</a>
                            </li>
                            <li class="nav-item m-auto">
                                <a class="nav-link navbarLink" href="{{ path('app_login') }}">Espace membres</a>
                            </li>
                        {% endif %}

                    </ul>
                    {% if app.user %}
                        <div class="nav-item">
                            <a class="nav-link navbarLink" href="{{ path('app_logout') }}">Déconnexion</a>
                        </div>
                    {% endif %}
                </div>

            </div>
        </nav>
